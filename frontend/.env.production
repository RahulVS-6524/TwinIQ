# TwinIQ Frontend - Production Environment
# Defaults to relative '/api' for Docker / Nginx reverse-proxy setups.
# For decoupled deployments (e.g. Vercel frontend + Render backend), set to the full URL:
# VITE_API_BASE_URL=https://twiniq-backend.onrender.com/api
VITE_API_BASE_URL=/api
